<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title class="titulo">Editar Película</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1 class="titulo">Película a Editar: <%= Movie.title %>
    </h1>
    <section class="formulario card w-50">
        <form action="/movies/update/<%= Movie.id %>?_method=PUT" method="POST">
            <div class="d-flex justify-content-center gap-10"><label for="title">Title </label>
                <p><input type="text" name="title" id="title" class="form-control w-100 p-2 <%= locals.errors && (errors.title && 'is-invalid') %>" value="<%= Movie.title %>"></p>
                <small class="text-danger d-flex justify-content-center gap-10">
                    <%= locals.errors && (errors.title && errors.title.msg) %>
                </small>
            </div>
            <div class="d-flex justify-content-center"><label for="rating">Rating </label>
                <p><input type="number" step="any" name="rating" id="rating" class="form-control w-100 p-2 <%= locals.errors && (errors.rating && 'is-invalid') %>" value="<%= Movie.rating %>"></p>
                <small class="text-danger d-flex justify-content-center gap-10">
                    <%= locals.errors && (errors.rating && errors.rating.msg) %>
                </small>
            </div>
            <div class="d-flex justify-content-center"><label for="awards">Awards </label>
                <p><input type="number" name="awards" id="awards" class="form-control w-100 p-2 <%= locals.errors && (errors.awards && 'is-invalid') %>" value="<%= Movie.awards %>"></p>
                <small class="text-danger d-flex justify-content-center gap-10">
                    <%= locals.errors && (errors.awards && errors.awards.msg) %>
                </small>
            </div>
            <div class="d-flex justify-content-center"><label for="release_date">Release date </label>
                <p><input type="date" name="release_date" id="release_date" class="form-control w-100 p-2 <%= locals.errors && (errors.release_date && 'is-invalid') %>"
                        value="<%= moment(Movie.release_date).format('YYYY-MM-DD') %>"></p>
                <small class="text-danger d-flex justify-content-center gap-10">
                    <%= locals.errors && (errors.release_date && errors.release_date.msg) %>
                </small>
            </div>
            <div class="d-flex justify-content-center"><label for="length">Length </label>
                <p><input type="number" name="length" id="length" class="form-control w-100 p-2 <%= locals.errors && (errors.length && 'is-invalid') %>" value="<%= Movie.length %>"></p>
                <small class="text-danger d-flex justify-content-center gap-10">
                    <%= locals.errors && (errors.length && errors.length.msg) %>
                </small>
            </div>
            <br>

            <div class="d-flex justify-content-center">

                <p> <button class="botonAgregar" type="submit">Actualizar</button>
    
                    <a class="botonVolver" href="/movies">Listado de Películas</a>
                </p>
            </div>
        </form>
    </section>
</body>

</html>