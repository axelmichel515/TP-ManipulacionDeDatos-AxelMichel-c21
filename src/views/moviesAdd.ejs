<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Movie add</title>
    <link rel='stylesheet' href='/css/style.css' />
</head>

<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
      </figure>
    <h1 class="titulo">Agregar Película</h1>
    <br>
    <section class="formulario card w-50">
        <form action="/movies/create" method="POST"  class="">
            <div class="d-flex justify-content-center gap-10">
            <label for="title">Title </label>
            <p class="mb-3"><input type="text" name="title" id="" class="form-control w-100 p-1 <%= locals.errors && (errors.title && 'is-invalid') %>"  value="<%= locals.old && (old.title && old.title ) %>"></p>
            </div>
            <div>
                <small class="text-danger d-flex justify-content-center gap-10">
                    <%= locals.errors && (errors.title && errors.title.msg) %>
                  </small>
            </div>
            <div class="d-flex justify-content-center gap-10">
            <label for="rating">Rating </label>
            <p><input type="number" step="any" name="rating" id="" class="form-control w-100 p-1 <%= locals.errors && (errors.rating && 'is-invalid') %>"  value="<%= locals.old && (old.rating&& old.rating ) %>"> </p>
            </div>
            <div>
                <small class="text-danger ms-2 d-flex justify-content-center gap-10">
                    <%= locals.errors && (errors.rating && errors.rating.msg) %>
                  </small>
            </div>
            <div class="d-flex justify-content-center gap-10">
            <label for="awards">Awards </label>
            <p><input type="number" name="awards" id="" class="form-control w-100 p-1  <%= locals.errors && (errors.awards && 'is-invalid') %>"  value="<%= locals.old && (old.awards && old.awards ) %>" ></p>
            </div>
            <div>
                <small class="text-danger ms-2 d-flex justify-content-center gap-10">
                    <%= locals.errors && (errors.awards && errors.awards.msg) %>
                  </small>
            </div>
            <div class="d-flex justify-content-center gap-10">
            <label for="release_date">Release date </label>
            <p><input type="date" name="release_date" id="" class="form-control w-100 p-1  <%= locals.errors && (errors.release_date && 'is-invalid') %>"  value="<%= locals.old && (old.release_date && old.release_date) %>"></p>
            </div>
            <div>
                <small class="text-danger ms-2 d-flex justify-content-center gap-10">
                    <%= locals.errors && (errors.release_date && errors.release_date.msg) %>
                  </small>
            </div>
            <div class="d-flex justify-content-center gap-10">
            <label for="length">Length </label>
            <p><input type="number" name="length" id="" class="form-control w-100 p-1  <%= locals.errors && (errors.length && 'is-invalid') %>"  value="<%= locals.old && (old.length && old.length ) %>"></p>
            </div>
            <div>
                <small class="text-danger ms-2 d-flex justify-content-center gap-10">
                    <%= locals.errors && (errors.length && errors.length.msg) %>
                  </small>
            </div>

            <br>
            <div class="d-flex justify-content-center">

                <p> <button class="botonAgregar" type="submit">Agregar</button>
                
                <a  href="/movies">Listado de Películas</a></p>
            </div>
        </form>
    </section>
    
</body>

</html>